{
  "name": "poke-auto",
  "version": "0.0.1",
  "private": true,
  "description": "Pokémon Center restock notifier (personal MVP)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/app/main.js",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "playwright": "1.59.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
